// Test that we can a.) create a block-area-module, that we can add blocks to it, b.) that we can save said block-area-module and get an id and c .) that we can use the block area block to render the block area module in another post using the id just created and that its contents are present and correct.
