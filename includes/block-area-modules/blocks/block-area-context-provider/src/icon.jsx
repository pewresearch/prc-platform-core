/**
 * External Dependencies
 */
import { symbol, Icon } from '@wordpress/icons';

export default function IconSymbolFilled({color = '#02b5d5'}) {
	return <Icon icon={ symbol } style={{
		color
	}}/>;
}
