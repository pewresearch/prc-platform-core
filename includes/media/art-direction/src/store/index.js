import reducer from './reducer';
import actions from './actions';
import selectors from './selectors';
import resolvers from './resolvers';

const controls = {};

export { reducer, actions, controls, selectors, resolvers };
